<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/panelBlueMain</item>
        <item name="colorPrimaryDark">@color/panelBlueDark</item>
        <item name="colorAccent">@color/textDark</item>
        <item name="android:colorBackground">@color/activityBackground</item>
    </style>

</resources>
